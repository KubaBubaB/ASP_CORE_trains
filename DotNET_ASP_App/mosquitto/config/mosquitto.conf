# Allow anonymous connections
allow_anonymous true

# Listen on all network interfaces on port 1883
listener 1883 0.0.0.0

# Optional: Enable WebSocket listener (if needed)
listener 9001
protocol websockets